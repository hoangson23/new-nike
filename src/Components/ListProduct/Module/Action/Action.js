import API from '../../../../Axios/API';
import * as ActionType from '../Constants/Constant';

export const createAction = ({type, payload}) => {
    return(
        type,
        payload
    )
}
// call API
export const actGetProduct = (gender, typeProduct) => {
    return async (dispatch) => {
        try{
            
        }catch(){

        }
    }
}